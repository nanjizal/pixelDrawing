package pixelDrawing.upkeep;
import pixelDrawing.Circle;
import pixelDrawing.Cubic;
import pixelDrawing.Fill;
import pixelDrawing.IPixelShape;
import pixelDrawing.Line;
import pixelDrawing.Plot;
import pixelDrawing.Quadratic;
import pixelDrawing.Rectangle;
import pixelDrawing.ShapeEnum;
import pixelDrawing.Triangle;

@:dox(hide)
class Doc {
    public static function main(){
        
    }
    public function new(){
        
    }
}